name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Setup Node.js 🔧
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install and build 🔧
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }} # Передаємо API-ключ як змінну середовища
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: dist
